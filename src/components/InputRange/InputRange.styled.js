import styled from 'styled-components';

const DefaultStyleInputRange = styled.input`
    width: 300px;
`

// const StyledNavButtonSize = styled(DefaultStyleNavButton)(({ size, theme }) => theme.button.size[size])
const StyledInputRange = DefaultStyleInputRange


export default StyledInputRange; 