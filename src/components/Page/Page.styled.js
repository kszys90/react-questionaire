import styled from 'styled-components';

const DefaultStylePage = styled.div`
    position: relative;
    padding: 30px;
    padding-top: 80px;
    text-align: center;
    display: flex;
    flex-direction: column;
    min-height: 540px;
`
const StyledPage = DefaultStylePage

export default StyledPage; 